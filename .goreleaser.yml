project_name: lowcode-lambda-go
release:
  github:
    owner: raywall
    name: aws-lowcode-lambda-go
  name_template: "{{ .Tag }}"
  draft: false
  prerelease: false
  goarch:
    - amd64
builds:
  - ldflags: -X main.version={{.Version}}
